# ---------- Java / Maven ----------
target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar

# ---------- 日志 ----------
logs/
*.log

# ---------- IDE ----------
.idea/
*.iml
*.ipr
*.iws
.vscode/

# ---------- Node / Vite ----------
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------- 系统 ----------
.DS_Store
Thumbs.db

# ---------- 密钥 ----------
# docker compose 从同目录 .env 读取环境变量（DASHSCOPE_API_KEY、JWT_SECRET），
# 模板见 .env.example。真实密钥只留本地，绝不进版本库
.env
.env.local

# TLS 证书（Cloudflare Origin Certificate 与本地自签证书）。
# 私钥绝不能进版本库；公钥虽然可以公开，但两份文件总是一起流转，一起排除更省心
certs/

# ---------- 运行时数据 ----------
# 知识库文件与上传分片的落盘位置（knowledge-base.file-storage-path / chunk-path）
data/

# ---------- 内部工作文档（不进入公开仓库）----------
# 包含简历包装策略、面试话术、项目缺陷盘点等内容，仅本地使用
CLAUDE.md
TODO.md
OPTIMIZATION-PLAN.md
