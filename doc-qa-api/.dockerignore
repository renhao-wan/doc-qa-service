# 构建阶段的上下文排除清单。
#
# ⚠️ target/ 必须排除：宿主机上的 target 里可能有上一次用别的方式打出来的 jar，
#    一旦被 COPY 进构建阶段，`mvn clean package` 虽会清掉它，但多拷一份大目录纯属浪费
#    （target 往往几百 MB，会显著拖慢构建上下文上传）。
target/

# 运行时数据：知识库文件与上传分片。属于应用状态，不该进镜像
data/
logs/

# IDE 与本地配置
.idea/
*.iml
.vscode/

# 说明性文件，运行时用不到
*.md
